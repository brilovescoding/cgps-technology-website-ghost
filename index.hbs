{{!< default}}
{{!-- The tag above means: insert everything in this file
into the {body} of the default.hbs template --}}
{{!-- test --}}
{{!-- test2 --}}
{{#is "home"}}
    {{#if @site.description}}
    <header class="page-head">
        <h2 class="page-head-title">{{@site.description}}</h2>
        {{!-- not sure if we need this --}} <p class="home-page-description">CGPS offers a wide variety of technology courses. Ranging from Web Design to Robotics, students have the ability to follow their interests and learn skills that are applicable in the real world.</p>
    </header>
    {{/if}}
{{/is}}

<div class="post-feed">
    {{#foreach posts}}

        {{!-- The tag below includes the markup for each post - partials/post-card.hbs --}}
        {{> "post-card"}}

    {{/foreach}}
</div>

